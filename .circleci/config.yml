version: 2.1
orbs:
  node: cimg/node:20.3.0
jobs:
  build-and-test:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - node/install-packages
      - run:
          command: npm test
workflows:
  build-and-test:
    jobs:
      - build-and-test